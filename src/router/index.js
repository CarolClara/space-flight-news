import Vue from 'vue'
import VueRouter from 'vue-router'

Vue.use(VueRouter)

const routes = [
  // Optei por não usar o `vue-router` no projeto
]

const router = new VueRouter({
  routes
})

export default router
